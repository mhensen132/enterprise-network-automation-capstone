all:
  children:
    kvm_hosts:
      hosts:
        ub1:
          ansible_host: 10.102.1.4
          ansible_user: mikeh
          ansible_ssh_private_key_file: ~/.ssh/id_ed25519

  vars:
    ansible_become: true